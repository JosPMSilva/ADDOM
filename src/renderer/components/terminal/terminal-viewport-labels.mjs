export function getTerminalViewportLabels(t) {
  return {
    sessionArchived: t('core:terminal.viewport.sessionState.archived', { defaultValue: 'Archived' }),
    sessionEnded: t('core:terminal.viewport.sessionState.ended', { defaultValue: 'Ended' }),
    sessionClosed: t('core:terminal.viewport.sessionState.closed', { defaultValue: 'Closed' }),
    sessionLive: t('core:terminal.viewport.sessionState.live', { defaultValue: 'Live' }),
    workspaceShell: t('core:terminal.viewport.workspaceShell', { defaultValue: 'Workspace shell' }),
    startTitle: t('core:terminal.viewport.startTitle', { defaultValue: 'Start a terminal session' }),
    unavailableTitle: t('core:terminal.viewport.unavailableTitle', { defaultValue: 'Chat terminal is unavailable' }),
    startDescription: t('core:terminal.viewport.startDescription', {
      defaultValue: 'Create a workspace shell to run interactive commands inside the real terminal surface.',
    }),
    unavailableDescription: t('core:terminal.viewport.unavailableDescription', {
      defaultValue: 'The PTY runtime is not available in this app state. Check the terminal runtime banner for details.',
    }),
    startingSession: t('core:terminal.viewport.startingSession', { defaultValue: 'Starting session...' }),
    createSession: t('core:terminal.viewport.createSession', { defaultValue: 'Create session' }),
    readOnly: t('core:terminal.viewport.readOnly', { defaultValue: 'Read-only' }),
    model: t('core:terminal.viewport.model', { defaultValue: 'Model' }),
    workspaceRoot: t('core:terminal.viewport.workspaceRoot', { defaultValue: 'workspace root' }),
    trimmed: t('core:terminal.viewport.trimmed', { defaultValue: 'Trimmed' }),
    clickToFocus: t('core:terminal.viewport.clickToFocus', { defaultValue: 'Click to focus' }),
    activateSurface: t('core:terminal.viewport.activateSurface', { defaultValue: 'Activate surface' }),
    archivedViewport: t('core:terminal.viewport.aria.archivedViewport', { defaultValue: 'Archived terminal viewport' }),
    interactiveViewport: t('core:terminal.viewport.aria.interactiveViewport', { defaultValue: 'Interactive terminal viewport' }),
    terminalActions: t('core:terminal.viewport.aria.terminalActions', { defaultValue: 'Terminal actions' }),
    cut: t('core:terminal.viewport.context.cut', { defaultValue: 'Cut selection' }),
    copy: t('core:terminal.viewport.context.copy', { defaultValue: 'Copy selection' }),
    copyVisibleOutput: t('core:terminal.viewport.context.copyVisibleOutput', { defaultValue: 'Copy visible output' }),
    copyFullScrollback: t('core:terminal.viewport.context.copyFullScrollback', { defaultValue: 'Copy full scrollback' }),
    sendOutputToChat: t('core:terminal.viewport.context.sendOutputToChat', { defaultValue: 'Send output to chat' }),
    explainLastError: t('core:terminal.viewport.context.explainLastError', { defaultValue: 'Explain last error' }),
    summarizeSession: t('core:terminal.viewport.context.summarizeSession', { defaultValue: 'Summarize session' }),
    saveSnapshotToMemory: t('core:terminal.viewport.context.saveSnapshotToMemory', { defaultValue: 'Save snapshot to Memory' }),
    paste: t('core:terminal.viewport.context.paste', { defaultValue: 'Paste' }),
    pasteAsSingleLine: t('core:terminal.viewport.context.pasteAsSingleLine', { defaultValue: 'Paste as single line' }),
    selectAll: t('core:terminal.viewport.context.selectAll', { defaultValue: 'Select all' }),
    clear: t('core:terminal.viewport.context.clear', { defaultValue: 'Clear' }),
    zoomIn: t('core:terminal.viewport.context.zoomIn', { defaultValue: 'Zoom in' }),
    zoomOut: t('core:terminal.viewport.context.zoomOut', { defaultValue: 'Zoom out' }),
    zoomReset: t('core:terminal.viewport.context.zoomReset', { defaultValue: 'Reset zoom' }),
    buffer: t('core:terminal.viewport.context.buffer', { defaultValue: 'Buffer' }),
    terminalFontSize: t('core:terminal.viewport.context.terminalFontSize', { defaultValue: '{{size}}px' }),
    searchPlaceholder: t('core:terminal.viewport.search.placeholder', { defaultValue: 'Search terminal output' }),
    searchPrevious: t('core:terminal.viewport.search.previous', { defaultValue: 'Previous match' }),
    searchNext: t('core:terminal.viewport.search.next', { defaultValue: 'Next match' }),
    searchClose: t('core:terminal.viewport.search.close', { defaultValue: 'Close search' }),
    searchNoMatches: t('core:terminal.viewport.search.noMatches', { defaultValue: 'No matches' }),
    searchResultCount: t('core:terminal.viewport.search.resultCount', { defaultValue: '{{current}}/{{total}}' }),
    cancel: t('core:terminal.common.cancel', { defaultValue: 'Cancel' }),
    largePasteTitle: t('core:terminal.viewport.pasteConfirm.title', { defaultValue: 'Confirm large paste' }),
    largePasteMessage: (lineCount) => t('core:terminal.viewport.pasteConfirm.message', {
      defaultValue: 'Paste {{count}} lines into the terminal?',
      count: lineCount,
    }),
  }
}
