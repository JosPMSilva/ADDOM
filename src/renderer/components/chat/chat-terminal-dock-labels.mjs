export function buildTerminalDockLabels(t) {
  return {
    terminal: t('core:terminal.common.terminalTitle', { defaultValue: 'Terminal' }),
    approval: t('core:terminal.dock.state.approval', { defaultValue: 'Approval' }),
    approvalSuffix: t('core:terminal.dock.state.approvalSuffix', { defaultValue: 'approval' }),
    denied: t('core:terminal.dock.state.denied', { defaultValue: 'Denied' }),
    failed: t('core:terminal.common.status.failed', { defaultValue: 'Failed' }),
    closing: t('core:terminal.common.status.closing', { defaultValue: 'Closing' }),
    ended: t('core:terminal.common.status.ended', { defaultValue: 'Ended' }),
    userTakeover: t('core:terminal.dock.state.userTakeover', { defaultValue: 'User takeover' }),
    aiWaiting: t('core:terminal.dock.state.aiWaiting', { defaultValue: 'AI waiting' }),
    aiControlling: t('core:terminal.dock.state.aiControlling', { defaultValue: 'AI controlling' }),
    running: t('core:terminal.dock.state.running', { defaultValue: 'Running' }),
    currentThread: t('core:terminal.dock.browser.sections.currentThread', { defaultValue: 'Current Thread' }),
    otherLive: t('core:terminal.dock.browser.sections.otherLive', { defaultValue: 'Other Live' }),
    history: t('core:terminal.dock.browser.sections.history', { defaultValue: 'History' }),
    userTakeoverActiveAnnouncement: t('core:terminal.dock.announcements.userTakeoverActive', { defaultValue: 'User takeover is active.' }),
    takeoverRequiredAnnouncement: t('core:terminal.dock.announcements.takeoverRequired', { defaultValue: 'Take over this terminal to start typing.' }),
    resizeDock: t('core:terminal.dock.resize', { defaultValue: 'Resize terminal dock' }),
    browserTitle: t('core:terminal.dock.browser.title', { defaultValue: 'Terminal browser' }),
    browserSubtitle: t('core:terminal.dock.browser.subtitle', { defaultValue: 'Current Thread / Other Live / History' }),
    tablistAriaLabel: t('core:terminal.dock.tablistAriaLabel', { defaultValue: 'Thread terminal sessions' }),
    unread: t('core:terminal.dock.unread', { defaultValue: 'Unread' }),
    openTerminal: t('core:terminal.dock.actions.openTerminal', { defaultValue: 'Open terminal' }),
    backToDock: t('core:terminal.dock.actions.backToDock', { defaultValue: 'Back to dock' }),
    browseSessions: t('core:terminal.dock.actions.browseSessions', { defaultValue: 'Browse sessions' }),
    handBackToAi: t('core:terminal.dock.actions.handBackToAi', { defaultValue: 'Hand back to AI' }),
    confirmTakeover: t('core:terminal.dock.actions.confirmTakeover', { defaultValue: 'Confirm takeover' }),
    takeOver: t('core:terminal.dock.actions.takeOver', { defaultValue: 'Take over' }),
    expandBrowser: t('core:terminal.dock.actions.expandBrowser', { defaultValue: 'Expand terminal browser' }),
    collapseBrowser: t('core:terminal.dock.actions.collapseBrowser', { defaultValue: 'Collapse terminal browser' }),
    expand: t('core:terminal.dock.actions.expand', { defaultValue: 'Expand' }),
    collapse: t('core:terminal.dock.actions.collapse', { defaultValue: 'Collapse' }),
    openTerminalActions: t('core:terminal.dock.actions.openTerminalActions', { defaultValue: 'Terminal menu' }),
    terminalDockActions: t('core:terminal.dock.actions.terminalDockActions', { defaultValue: 'Terminal dock actions' }),
    closeBrowser: t('core:terminal.dock.actions.closeBrowser', { defaultValue: 'Close browser' }),
    renameSession: t('core:terminal.dock.actions.renameSession', { defaultValue: 'Rename session' }),
    editTitle: t('core:terminal.dock.actions.editTitle', { defaultValue: 'Edit title' }),
    duplicateSession: t('core:terminal.dock.actions.duplicateSession', { defaultValue: 'Duplicate session' }),
    sameCwdShell: t('core:terminal.dock.actions.sameCwdShell', { defaultValue: 'Same cwd and shell' }),
    sendOutputToChat: t('core:terminal.dock.browser.actions.sendOutputToChat', { defaultValue: 'Send output to chat' }),
    explainLastError: t('core:terminal.dock.browser.actions.explainLastError', { defaultValue: 'Explain last error' }),
    summarizeSession: t('core:terminal.dock.browser.actions.summarizeSession', { defaultValue: 'Summarize session' }),
    saveSnapshotToMemory: t('core:terminal.dock.browser.actions.saveSnapshotToMemory', { defaultValue: 'Save snapshot to Memory' }),
    prefillComposer: t('core:terminal.dock.browser.actions.prefillComposer', { defaultValue: 'Prefill composer' }),
    recentOutput: t('core:terminal.dock.browser.actions.recentOutput', { defaultValue: 'Recent output' }),
    boundedOutput: t('core:terminal.dock.browser.actions.boundedOutput', { defaultValue: 'Bounded output' }),
    threadMemory: t('core:terminal.dock.browser.actions.threadMemory', { defaultValue: 'Thread memory' }),
    newAtProjectRoot: t('core:terminal.dock.actions.newAtProjectRoot', { defaultValue: 'New at project root' }),
    newFromCurrentCwd: t('core:terminal.dock.actions.newFromCurrentCwd', { defaultValue: 'New from current cwd' }),
    newFromEditorCwd: t('core:terminal.dock.actions.newFromEditorCwd', { defaultValue: 'New from editor folder' }),
    currentCwd: t('core:terminal.dock.actions.currentCwd', { defaultValue: 'Current cwd' }),
    editorFolder: t('core:terminal.dock.actions.editorFolder', { defaultValue: 'Editor folder' }),
    defaultShell: t('core:terminal.dock.actions.defaultShell', { defaultValue: 'Default shell' }),
    newWithShell: t('core:terminal.dock.actions.newWithShell', { defaultValue: 'New with shell' }),
    interrupt: t('core:terminal.dock.actions.interrupt', { defaultValue: 'Interrupt' }),
    stopInput: t('core:terminal.dock.actions.stopInput', { defaultValue: 'Stop input' }),
    closeSession: t('core:terminal.dock.actions.closeSession', { defaultValue: 'Close session' }),
    archiveSession: t('core:terminal.dock.actions.archiveSession', { defaultValue: 'Archive session' }),
    hideTerminal: t('core:terminal.dock.actions.hideTerminal', { defaultValue: 'Hide terminal' }),
    dismiss: t('core:terminal.dock.actions.dismiss', { defaultValue: 'Dismiss' }),
    forceTerminate: t('core:terminal.dock.actions.forceTerminate', { defaultValue: 'Force terminate' }),
    killProcess: t('core:terminal.dock.actions.killProcess', { defaultValue: 'Kill process' }),
    terminalSessions: t('core:terminal.dock.terminalSessions', { defaultValue: 'Terminal Sessions' }),
    renamePrompt: t('core:terminal.dock.prompts.renameSession', { defaultValue: 'Rename terminal session' }),
    save: t('core:terminal.common.save', { defaultValue: 'Save' }),
    forceTerminateHostTitle: t('core:terminal.dock.confirm.forceTerminateHost.title', { defaultValue: 'Force Terminate Host Terminal' }),
    forceTerminateTitle: t('core:terminal.dock.confirm.forceTerminate.title', { defaultValue: 'Force Terminate Terminal' }),
    forceTerminateHostMessage: t('core:terminal.dock.confirm.forceTerminateHost.message', {
      defaultValue: 'Force terminate this host-scoped terminal session? This will immediately kill the underlying process.',
    }),
    forceTerminateMessage: t('core:terminal.dock.confirm.forceTerminate.message', {
      defaultValue: 'Force terminate this terminal session? This will immediately kill the underlying process.',
    }),
    cancel: t('core:terminal.common.cancel', { defaultValue: 'Cancel' }),
    aiControlsShell: t('core:terminal.dock.control.aiControlsShell', { defaultValue: 'AI controls this shell. Take over before typing.' }),
    userControlsShell: t('core:terminal.dock.control.userControlsShell', { defaultValue: 'You currently control this shell.' }),
    typingBlockedUntilTakeover: t('core:terminal.dock.control.typingBlockedUntilTakeover', { defaultValue: 'Typing is blocked until you confirm takeover.' }),
    aiWaitingForUser: t('core:terminal.dock.control.aiWaitingForUser', { defaultValue: 'AI is waiting for user confirmation before continuing.' }),
    deleteArchiveTitle: t('core:terminal.dock.confirm.deleteArchive.title', { defaultValue: 'Delete Archived Terminal Session' }),
    deleteArchiveMessage: t('core:terminal.dock.confirm.deleteArchive.message', {
      defaultValue: 'Delete this archived terminal session?\n\nThis removes the retained archive record only and cannot be undone.',
    }),
    deleteArchiveConfirm: t('core:terminal.dock.confirm.deleteArchive.confirm', { defaultValue: 'Delete Archive' }),
  }
}

export function buildTerminalDockBrowserEntryLabels(t, dockLabels) {
  return {
    workspaceRoot: t('core:terminal.dock.workspaceRoot', { defaultValue: 'workspace root' }),
    requestedIn: t('core:terminal.dock.browser.requestedIn', {
      defaultValue: 'Requested in {{section}}',
      section: dockLabels.currentThread,
    }),
    terminalSessionLabels: {
      user: t('core:terminal.common.user', { defaultValue: 'User' }),
      ai: t('core:terminal.common.ai', { defaultValue: 'AI' }),
      session: t('core:terminal.common.session', { defaultValue: 'Session' }),
      host: t('core:terminal.common.host', { defaultValue: 'Host' }),
      workspace: t('core:terminal.common.workspace', { defaultValue: 'Workspace' }),
      shell: t('core:terminal.common.shell', { defaultValue: 'shell' }),
      terminal: t('core:terminal.common.terminal', { defaultValue: 'terminal' }),
      threadPrefix: t('core:terminal.common.threadPrefix', { defaultValue: 'Thread' }),
      closing: dockLabels.closing,
      ended: dockLabels.ended,
      closed: t('core:terminal.common.status.closed', { defaultValue: 'Closed' }),
      live: t('core:terminal.common.status.live', { defaultValue: 'Live' }),
      failed: dockLabels.failed,
      terminated: t('core:terminal.common.status.terminated', { defaultValue: 'Terminated' }),
      closedPrefix: t('core:terminal.common.timestamp.closedPrefix', { defaultValue: 'Closed' }),
      startedPrefix: t('core:terminal.common.timestamp.startedPrefix', { defaultValue: 'Started' }),
      savedToMemory: t('core:terminal.dock.browser.actions.savedToMemory', { defaultValue: 'Saved to Memory' }),
      dismissed: t('core:terminal.common.suggestion.dismissed', { defaultValue: 'Dismissed' }),
      suggested: t('core:terminal.common.suggestion.suggested', { defaultValue: 'Suggested' }),
      noSuggestion: t('core:terminal.common.suggestion.none', { defaultValue: 'No suggestion' }),
    },
    dockStateLabels: dockLabels,
  }
}
